title: Senat Elections
new-page: true
tags: event
priority: -2
view-if: (year = next_election_year and month >= next_election_month) or (year > next_election_year)
on-departure: month_actions += 1; in_election = 0; next_election_year = next_election_year + 4; n_elections += 1; next_election_time = time + 48; papen_chancellor_timer = 4;
face-image: img/fl_pl.webp

= Elections, [+ month : month +] [+ year +]

The Senat elections are to be held now.

- @senat: May we do our best...

@senat
on-arrival: {!
var senat_adjustment_factor = {
    kpp: 0.3,
    pps: 0.7,
    npr: 1.0,
    psl_l: 0.3,
    psl_w: 0.7,
    psl_p: 1.0,
    bmn: 1.2,
    zln: 1.4,
    pschd: 0.9,
    other: 0.2,
    otherright: 0.3,
};

Q.kpp_sn_votes = Q.kpp_votes*senat_adjustment_factor.kpp;
Q.pps_sn_votes = Q.pps_votes*senat_adjustment_factor.pps;
Q.npr_sn_votes = Q.npr_votes*senat_adjustment_factor.npr;
Q.psl_l_sn_votes = Q.psl_l_votes*senat_adjustment_factor.psl_l;
Q.psl_w_sn_votes = Q.psl_w_votes*senat_adjustment_factor.psl_w;
Q.psl_p_sn_votes = Q.psl_p_votes*senat_adjustment_factor.psl_p;
Q.bmn_sn_votes = Q.bmn_votes*senat_adjustment_factor.bmn;
Q.zln_sn_votes = Q.zln_votes*senat_adjustment_factor.zln;
Q.pschd_sn_votes = Q.pschd_votes*senat_adjustment_factor.pschd;
Q.other_sn_votes = Q.other_votes*senat_adjustment_factor.other;
Q.otherright_sn_votes = Q.otherright_votes*senat_adjustment_factor.otherright;

Q.total_votes_sn = Q.npr_sn_votes + Q.pps_sn_votes + Q.npr_sn_votes + Q.psl_l_sn_votes + Q.psl_w_sn_votes + Q.psl_p_sn_votes + Q.bmn_sn_votes + Q.zln_sn_votes + Q.pschd_sn_votes + Q.other_sn_votes + Q.otherright_sn_votes

if (Q.total_votes_sn > 0) {
    Q.kpp_sn_votes = (Q.kpp_sn_votes/Q.total_votes_sn)*100;
    Q.pps_sn_votes = (Q.pps_sn_votes/Q.total_votes_sn)*100;
    Q.npr_sn_votes = (Q.npr_sn_votes/Q.total_votes_sn)*100;
    Q.psl_l_sn_votes = (Q.psl_l_sn_votes/Q.total_votes_sn)*100;
    Q.psl_p_sn_votes = (Q.psl_w_sn_votes/Q.total_votes_sn)*100;
    Q.psl_w_sn_votes = (Q.psl_p_sn_votes/Q.total_votes_sn)*100;
    Q.bmn_sn_votes = (Q.bmn_sn_votes/Q.total_votes_sn)*100;
    Q.zln_sn_votes = (Q.zln_sn_votes/Q.total_votes_sn)*100;
    Q.pschd_sn_votes = (Q.pschd_sn_votes/Q.total_votes_sn)*100;
    Q.other_sn_votes = (Q.other_sn_votes/Q.total_votes_sn)*100;
    Q.otherright_sn_votes = (Q.otherright_sn_votes/Q.total_votes_sn)*100;
}

var new_party_sum = 0;
for (var party of Q.parties) {
    Q['old_' + party + '_sn'] = Q[party + '_sn'];
    if (Q[party + '_sn_votes'] < Q.electoral_threshold || (Q.electoral_threshold > 1 && party == 'other') || (Q.electoral_threshold > 1 && party == 'otherright') || Q[party + '_banned'] || Q[party + '_boycott']) {
        Q[party + '_sn'] = 0;
    } else {
        Q[party + '_sn'] = Q[party + '_sn_votes'];
    }
    new_party_sum += Q[party + '_sn'];
    Q[party + '_sn_disp'] = Q[party + '_sn'].toFixed(1);
    Q['change_' + party + '_sn'] = Q[party + '_sn'] - Q['old_' + party + '_sn'];
    var sign = Q['change_' + party + '_sn'] > 0 
                   ? '<img src="img/arrowup.png"> ' 
                   : Q['change_' + party + '_sn'] < 0 
                     ? '<img src="img/arrowdown.png"> ' 
                     : '<img src="img/steady.png"> ';
    Q['str_change_' + party] = sign + Q['change_' + party + '_sn'].toFixed(1);
};
!}
